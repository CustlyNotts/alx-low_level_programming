#include "main.h"
#include <string.h>
/**
 * _strlen - gets length
 * @s: the argument
 * Return: integer length of string
 */
int _strlen(char *s)
{
	int k;
	char strap[] = s;

	k = strlen(strap);
	return (k);
}
